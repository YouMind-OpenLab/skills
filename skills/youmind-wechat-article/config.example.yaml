# YouMind WeChat Skill Configuration

# YouMind OpenAPI — knowledge base search, web search, article archiving,
# image generation (via YouMind chat API), and (future) WeChat publishing
# proxy. The same key is used for all YouMind-mediated operations.
youmind:
  api_key: ""              # sk-ym-xxxxxxxxxxxxxxxxxxxx
  base_url: "https://youmind.com/openapi/v1"

# Default theme formatting (not credentials — safe to commit)
theme: "simple"             # simple | center | decoration | prominent
theme_color: "#3498db"      # Any HEX color or preset name
